services:
  costco-gas-scraper:
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
    networks:
      - nathanlai-shared

networks:
  nathanlai-shared:
    external: true
